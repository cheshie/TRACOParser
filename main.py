from src import FileBuilder
from src import logger

if __name__ == "__main__":
    parsing_phrases = 'ddfhfhfahkafkh'  # here is place for object from Parser
    filename = 'main'
    FileBuilder(parsing_phrases, filename)
